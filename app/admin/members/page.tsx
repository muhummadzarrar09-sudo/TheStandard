'use client';import{useState}from'react';import Link from'next/link';import {MemberForm} from '../../../components/admin/AdminForms'
export default function Members(){const[members,setMembers]=useState([{email:'alex@example.com',status:'Active',team:'Northstar'},{email:'jordan@example.com',status:'Active',team:'Northstar'},{email:'sam@example.com',status:'Invited',team:'Northstar'}]);const[email,setEmail]=useState('');return <main className="main"><p className="eyebrow">ADMIN · MEMBERS</p><h1>Run the cohort.</h1><p className="muted">Provision access manually. No external membership dependency.</p><section className="card" style={{marginTop:30}}><div style={{display:'flex',gap:8}}><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="member@email.com" style={{flex:1,padding:12,background:'var(--bg)',border:'1px solid var(--line)',color:'var(--text)'}}/><button className="button" onClick={async()=>{if(email){const r=await fetch('/api/admin/members',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});if(r.ok)setMembers([...members,{email,status:'Invited',team:'Unassigned'}]);setEmail('')}}}>Invite member</button></div>{members.map(m=><div key={m.email} style={{display:'flex',justifyContent:'space-between',padding:'17px 0',borderBottom:'1px solid var(--line)'}}><div><b>{m.email}</b><p className="muted" style={{margin:'4px 0 0'}}>{m.team}</p></div><span style={{color:m.status==='Active'?'var(--accent)':'var(--muted)'}}>{m.status}</span></div>)}</section></main>}
