'use client';import{useState}from'react'
export default function Enrollment(){const[open,setOpen]=useState(false);return <main className="main"><p className="eyebrow">ADMIN · ENROLLMENT</p><h1>Control the window.</h1><section className="card" style={{marginTop:30}}><p className="eyebrow">OTP REGISTRATION</p><h2 style={{color:open?'var(--accent)':'var(--muted)'}}>{open?'OPEN':'CLOSED'}</h2><p className="muted">{open?'Eligible emails may request a six-digit code.':'No new signup OTPs will be issued. Existing members retain access.'}</p><button className="button" onClick={()=>setOpen(!open)}>{open?'Close enrollment':'Open enrollment'}</button></section></main>}
