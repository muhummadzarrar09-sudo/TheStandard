'use client'
import {useState} from 'react';import Link from 'next/link';import {themes} from '../../../themes';import PushSubscription from '../../../components/pwa/PushSubscription'
export default function Settings(){const [selected,setSelected]=useState('whoop-oura');return <div className="shell"><aside className="rail"><div className="brand">DISCIPLINE<small>EXECUTION SYSTEM</small></div><nav><Link href="/dashboard">Today</Link><Link href="/team">Team room</Link><Link href="/reports">Reports</Link><Link className="active" href="/settings">Settings</Link></nav></aside><main className="main"><p className="eyebrow">ACCOUNT · APPEARANCE</p><h1>Set your environment.</h1><section className="card" style={{marginTop:32}}><p className="eyebrow">STYLE PRESET</p><p className="muted">The structure stays the same. The way it feels is yours.</p><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:10,marginTop:24}}>{Object.entries(themes).map(([key,t])=><button key={key} onClick={()=>setSelected(key)} style={{textAlign:'left',padding:16,border:selected===key?'1px solid var(--accent)':'1px solid var(--line)',background:t.background,color:t.text,borderRadius:t.radius,cursor:'pointer',minHeight:120}}><b>{t.name}</b><small style={{display:'block',color:t.muted,marginTop:8}}>{t.density} · {t.font}</small><i style={{display:'block',background:t.accent,width:24,height:4,marginTop:22}}/></button>)}</div></section><section className="grid" style={{marginTop:15}}><PushSubscription/><div className="card"><p className="eyebrow">SECURITY</p><Link href="/settings/devices">Manage active devices →</Link></div></section></main></div>}
