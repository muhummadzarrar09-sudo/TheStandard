import Link from'next/link';import{getPublishedReports}from'../../../lib/content/queries'
export default async function Reports(){const reports=await getPublishedReports();const latest=reports[0];return <div className="shell"><aside className="rail"><div className="brand">DISCIPLINE<small>EXECUTION SYSTEM</small></div><nav><Link href="/dashboard">Today</Link><Link href="/team">Team room</Link><Link className="active" href="/reports">Reports</Link><Link href="/community">Community</Link><Link href="/settings">Settings</Link></nav></aside><main className="main"><p className="eyebrow">INTELLIGENCE LIBRARY · INTERVIEWS</p><h1>Study the standard.</h1>{latest?<div className="card" style={{marginTop:32,background:'linear-gradient(140deg,#20301c,#111716)'}}><p className="eyebrow">LATEST INTERVIEW · {new Date(latest.published_at).toLocaleDateString()}</p><h2 style={{fontSize:34,maxWidth:550}}>{latest.title}</h2><p className="muted">{latest.summary}</p><div style={{borderTop:'1px solid #ffffff1a',paddingTop:15,marginTop:25}}><Link href={`/reports/${latest.id}`} style={{color:'var(--accent)'}}>OPEN REPORT →</Link></div></div>:<section className="card" style={{marginTop:32}}><p className="muted">No reports have been published yet. Return when the first interview is ready.</p></section>}<p className="eyebrow" style={{marginTop:45}}>ARCHIVE</p>{reports.slice(1).map(r=><Link className="card" style={{marginTop:10,display:'flex',justifyContent:'space-between',gap:20}} href={`/reports/${r.id}`} key={r.id}><b>{r.title}</b><span className="muted">{new Date(r.published_at).toLocaleDateString()}</span></Link>)}</main></div>}
