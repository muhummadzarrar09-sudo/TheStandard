'use client'
import{useState}from'react';import{createSupabaseBrowser}from'../../lib/supabase/browser'
function keyToBytes(key:string){const pad='='.repeat((4-key.length%4)%4);const base=(key+pad).replace(/-/g,'+').replace(/_/g,'/');const raw=atob(base);return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)))}
export default function PushSubscription(){const[state,setState]=useState<'idle'|'working'|'enabled'|'unsupported'|'denied'>('idle');async function enable(){if(!('serviceWorker'in navigator)||!('PushManager'in window)||!('Notification'in window)){setState('unsupported');return}if(!process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY){setState('unsupported');return}setState('working');const permission=await Notification.requestPermission();if(permission!=='granted'){setState('denied');return}const registration=await navigator.serviceWorker.ready;const existing=await registration.pushManager.getSubscription();const subscription=existing||await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:keyToBytes(process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY||'')});const json=subscription.toJSON();const db=createSupabaseBrowser();const{data:{session}}=await db.auth.getSession();const r=await fetch('/api/push/subscribe',{method:'POST',headers:{'content-type':'application/json',...(session?.access_token?{authorization:`Bearer ${session.access_token}`}:{})},body:JSON.stringify({endpoint:json.endpoint,keys:json.keys})});setState(r.ok?'enabled':'idle')}return <div className="card"><p className="eyebrow">NOTIFICATIONS</p><h3>Keep your commitments visible.</h3><p className="muted">Receive local-time reminders and new report alerts. Permission is requested only when you choose.</p><button className="button" onClick={enable} disabled={state==='working'||state==='enabled'}>{state==='working'?'Enabling…':state==='enabled'?'Notifications enabled':state==='denied'?'Permission denied':state==='unsupported'?'Not supported here':'Enable notifications →'}</button></div>}
