'use client';import{useState}from'react'
export function MemberForm(){const[email,setEmail]=useState('');const[message,setMessage]=useState('');return <form onSubmit={async e=>{e.preventDefault();const r=await fetch('/api/admin/members',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});setMessage(r.ok?'Member provisioned.':'Could not provision member.');if(r.ok)setEmail('')}}><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="member@email.com" required type="email"/><button className="button">Provision member</button>{message&&<small className="muted">{message}</small>}</form>}
export function ReportForm(){const[title,setTitle]=useState('');const[summary,setSummary]=useState('');const[message,setMessage]=useState('');return <form onSubmit={async e=>{e.preventDefault();const r=await fetch('/api/admin/reports',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({title,summary})});setMessage(r.ok?'Report published.':'Could not publish report.');if(r.ok){setTitle('');setSummary('')}}><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Report title" required/><textarea value={summary} onChange={e=>setSummary(e.target.value)} placeholder="Mobile-readable summary" required/><button className="button">Publish report</button>{message&&<small className="muted">{message}</small>}</form>}
