'use client';import{useEffect,useState}from'react'
export default function DailyCheckin(){const[reflection,setReflection]=useState('');const[completed,setCompleted]=useState(false);const[state,setState]=useState('');useEffect(()=>{fetch('/api/checkins').then(r=>r.json()).then(x=>{const c=x.checkins?.[0];if(c){setCompleted(c.completed);setReflection(c.reflection_private||'')}})},[]);async function save(){setState('Saving…');const timezone=Intl.DateTimeFormat().resolvedOptions().timeZone;const r=await fetch('/api/checkins',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({completed,reflection,timezone})});setState(r.ok?'Saved':'Could not save')}return <section className="card" style={{marginTop:15}}><p className="eyebrow">19:00 · DAILY CHECK-IN</p><h3>Close the day deliberately.</h3><label style={{display:'flex',gap:10,alignItems:'center',margin:'18px 0'}}><input type="checkbox" checked={completed} onChange={e=>setCompleted(e.target.checked)}/> I completed today’s required standard.</label><textarea value={reflection} onChange={e=>setReflection(e.target.value)} maxLength={5000} placeholder="What did you execute? What changes tomorrow?" style={{width:'100%',minHeight:110,padding:12,background:'var(--bg)',border:'1px solid var(--line)',color:'var(--text)'}}/><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginTop:12}}><small className="muted">Private reflection · {reflection.length}/5000</small><button className="button" onClick={save}>{state||'Save check-in'}</button></div></section>}
