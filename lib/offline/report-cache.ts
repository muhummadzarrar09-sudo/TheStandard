const CACHE='discipline-reports-v1';export async function cacheReport(id:string,response:Response){const c=await caches.open(CACHE);await c.put(`/reports/${id}`,response.clone())}export async function getCachedReport(id:string){const c=await caches.open(CACHE);return c.match(`/reports/${id}`)}export async function clearReportCache(){const keys=await caches.keys();await Promise.all(keys.filter(k=>k.startsWith('discipline-reports-')).map(k=>caches.delete(k)))}
