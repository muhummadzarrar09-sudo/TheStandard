import{createClient}from'https://esm.sh/@supabase/supabase-js@2';Deno.serve(async req=>{if(req.headers.get('x-cron-secret')!==Deno.env.get('CRON_SECRET'))return new Response('Unauthorized',{status:401});const db=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const{data:jobs}=await db.from('notification_jobs').select('id,user_id,category,payload').eq('status','queued').lte('scheduled_at',new Date().toISOString()).limit(100);for(const job of jobs||[])await db.from('notification_jobs').update({status:'processing',attempts:1}).eq('id',job.id);return Response.json({ok:true,queued:jobs?.length||0})});
